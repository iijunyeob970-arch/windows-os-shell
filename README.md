# windows-os-shell
windows os shell프젝 입니다
Java + Gradle 기반이며, 소스코드 폴더에서 CMD를 열고 아래 명령어를 입력하면 빌드됩니다.

```bat
gradlew.bat build
```

성공 시:

```text
BUILD SUCCESSFUL
```

실행은:

```bat
gradlew.bat run
```

아직 Alpha 개발 중입니다.
